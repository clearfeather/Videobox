<Page
    x:Class="Screenbox.Pages.HomePage"
    x:Name="PageRoot"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:behaviors="using:Screenbox.Behaviors"
    xmlns:commands="using:Screenbox.Commands"
    xmlns:controls="using:Screenbox.Controls"
    xmlns:ctAnimations="using:CommunityToolkit.WinUI.Animations"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:extensions="using:Screenbox.Extensions"
    xmlns:helpers="using:Screenbox.Helpers"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:Screenbox.Core.Models"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:strings="using:Screenbox.Strings"
    xmlns:triggers="using:CommunityToolkit.WinUI"
    xmlns:ui="using:CommunityToolkit.WinUI"
    xmlns:viewModels="using:Screenbox.Core.ViewModels"
    Loaded="{x:Bind ViewModel.OnLoaded}"
    mc:Ignorable="d">

    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Controls/Templates/GridViewItemTemplates.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <MenuFlyout x:Key="ItemFlyout">
                <interactivity:Interaction.Behaviors>
                    <behaviors:AddToPlaylistFlyoutBehavior TargetSubItemName="AddToPlaylistSubMenu" />
                </interactivity:Interaction.Behaviors>

                <MenuFlyoutItem
                    Command="{x:Bind ViewModel.PlayCommand}"
                    CommandParameter="{Binding}"
                    Icon="{ui:FontIcon FontFamily={StaticResource ScreenboxSymbolThemeFontFamily},
                                       Glyph={StaticResource PlayGlyph}}"
                    Text="{strings:Resources Key=Play}" />
                <MenuFlyoutItem
                    Command="{x:Bind Common.PlayNextCommand}"
                    CommandParameter="{Binding}"
                    Icon="{ui:FontIcon FontFamily={StaticResource ScreenboxSymbolThemeFontFamily},
                                       Glyph={StaticResource PlayAddGlyph}}"
                    Text="{strings:Resources Key=PlayNext}" />
                <MenuFlyoutItem
                    Command="{x:Bind Common.AddToQueueCommand}"
                    CommandParameter="{Binding}"
                    Icon="{ui:FontIcon FontFamily={StaticResource ScreenboxSymbolThemeFontFamily},
                                       Glyph={StaticResource PlaylistMusicGlyph}}"
                    Text="{strings:Resources Key=AddToQueue}" />
                <MenuFlyoutSeparator />
                <MenuFlyoutItem
                    Command="{x:Bind Common.AddToFavoritesCommand}"
                    CommandParameter="{Binding}"
                    Icon="{ui:FontIcon Glyph=&#xE734;}"
                    Text="{strings:Resources Key=AddToFavorites}" />
                <MenuFlyoutSubItem
                    x:Name="AddToPlaylistSubMenu"
                    Icon="{ui:SymbolIcon Symbol=Add}"
                    Text="{strings:Resources Key=AddToPlaylist}" />
                <MenuFlyoutItem
                    Command="{StaticResource OpenWithCommand}"
                    CommandParameter="{Binding}"
                    Icon="{ui:FontIcon Glyph=&#xE7AC;}"
                    Text="{x:Bind strings:Resources.OpenWith}" />
                <MenuFlyoutItem
                    Command="{x:Bind ViewModel.RemoveCommand}"
                    CommandParameter="{Binding}"
                    Icon="{ui:SymbolIcon Symbol=Clear}"
                    Text="{strings:Resources Key=Remove}" />
                <MenuFlyoutItem
                    Command="{StaticResource ShowPropertiesCommand}"
                    CommandParameter="{Binding}"
                    Icon="{ui:FontIcon Glyph=&#xE946;}"
                    Text="{strings:Resources Key=Properties}" />
                <MenuFlyoutItem
                    Command="{x:Bind Common.OpenAlbumCommand}"
                    CommandParameter="{Binding Album}"
                    Icon="{ui:FontIcon Glyph=&#xE93C;}"
                    Text="{strings:Resources Key=ShowAlbum}"
                    Visibility="{Binding Album, Converter={StaticResource EmptyObjectToVisibilityConverter}}" />
                <MenuFlyoutItem
                    Command="{x:Bind Common.OpenArtistCommand}"
                    CommandParameter="{Binding MainArtist}"
                    Icon="{ui:FontIcon Glyph=&#xE77B;}"
                    Text="{strings:Resources Key=ShowArtist}"
                    Visibility="{Binding MainArtist, Converter={StaticResource EmptyObjectToVisibilityConverter}}" />
                <MenuFlyoutSeparator Visibility="{x:Bind Common.IsAdvancedModeEnabled}" />
                <MenuFlyoutItem
                    CommandParameter="{Binding}"
                    Icon="{ui:SymbolIcon Symbol=Setting}"
                    Text="{strings:Resources Key=SetPlaybackOptions}"
                    Visibility="{x:Bind Common.IsAdvancedModeEnabled}">
                    <MenuFlyoutItem.Command>
                        <commands:SetPlaybackOptionsCommand PlayCommand="{x:Bind ViewModel.PlayCommand}" />
                    </MenuFlyoutItem.Command>
                </MenuFlyoutItem>
            </MenuFlyout>

            <MenuFlyout x:Key="OpenFlyout" Placement="BottomEdgeAlignedRight">
                <MenuFlyoutItem
                    AccessKey="{strings:KeyboardResources Key=MenuItemFileKey}"
                    Command="{x:Bind Common.OpenFilesCommand}"
                    Icon="{ui:FontIcon FontFamily={StaticResource ScreenboxSymbolThemeFontFamily},
                                       Glyph={StaticResource FolderOpenGlyph}}"
                    Text="{strings:Resources Key=OpenFiles}" />
                <MenuFlyoutItem
                    x:Name="OpenMenuFlyoutFolderItem"
                    AccessKey="{strings:KeyboardResources Key=MenuItemFolderKey}"
                    Command="{x:Bind ViewModel.OpenFolderCommand}"
                    Icon="{ui:FontIcon FontFamily={StaticResource ScreenboxSymbolThemeFontFamily},
                                       Glyph={StaticResource FolderListGlyph}}"
                    Text="{strings:Resources Key=OpenFolder}" />
            </MenuFlyout>

            <Style x:Key="DashboardTileButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="{ThemeResource ControlFillColorDefaultBrush}" />
                <Setter Property="BorderBrush" Value="{ThemeResource ControlStrokeColorDefaultBrush}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="Foreground" Value="{ThemeResource TextFillColorPrimaryBrush}" />
                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                <Setter Property="VerticalContentAlignment" Value="Stretch" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border
                                x:Name="Chrome"
                                MinWidth="{TemplateBinding MinWidth}"
                                MinHeight="{TemplateBinding MinHeight}"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                                <ContentPresenter
                                    x:Name="ContentPresenter"
                                    Margin="{TemplateBinding Padding}"
                                    HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                    Content="{TemplateBinding Content}"
                                    ContentTemplate="{TemplateBinding ContentTemplate}"
                                    ContentTransitions="{TemplateBinding ContentTransitions}"
                                    Foreground="{TemplateBinding Foreground}" />
                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroup x:Name="CommonStates">
                                        <VisualState x:Name="Normal" />
                                        <VisualState x:Name="PointerOver">
                                            <VisualState.Setters>
                                                <Setter Target="Chrome.Background" Value="{ThemeResource ControlFillColorSecondaryBrush}" />
                                                <Setter Target="Chrome.BorderBrush" Value="{ThemeResource ControlStrokeColorDefaultBrush}" />
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="Pressed">
                                            <VisualState.Setters>
                                                <Setter Target="Chrome.Background" Value="{ThemeResource ControlFillColorTertiaryBrush}" />
                                                <Setter Target="Chrome.BorderBrush" Value="{ThemeResource ControlStrokeColorDefaultBrush}" />
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="Disabled">
                                            <VisualState.Setters>
                                                <Setter Target="Chrome.Background" Value="{ThemeResource ControlFillColorDisabledBrush}" />
                                                <Setter Target="Chrome.BorderBrush" Value="{ThemeResource ControlStrokeColorDefaultBrush}" />
                                                <Setter Target="ContentPresenter.Foreground" Value="{ThemeResource TextFillColorDisabledBrush}" />
                                            </VisualState.Setters>
                                        </VisualState>
                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <DataTemplate x:Key="DashboardTileTemplate" x:DataType="models:HomeDashboardTile">
                <Button
                    Width="236"
                    MinHeight="88"
                    Padding="12"
                    HorizontalContentAlignment="Stretch"
                    Command="{Binding DataContext.OpenDashboardTileCommand, ElementName=PageRoot}"
                    CommandParameter="{Binding}"
                    Style="{StaticResource DashboardTileButtonStyle}">
                    <Grid ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <FontIcon
                            Width="36"
                            Height="36"
                            FontSize="24"
                            Glyph="{x:Bind Glyph}" />
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock
                                MaxLines="1"
                                Style="{StaticResource BodyStrongTextBlockStyle}"
                                Text="{x:Bind Title}"
                                TextTrimming="CharacterEllipsis" />
                            <TextBlock
                                MaxLines="2"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                Style="{StaticResource CaptionTextBlockStyle}"
                                Text="{x:Bind Subtitle}"
                                TextTrimming="WordEllipsis"
                                TextWrapping="WrapWholeWords" />
                        </StackPanel>
                    </Grid>
                </Button>
            </DataTemplate>
        </ResourceDictionary>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid
            x:Name="HeaderGrid"
            Grid.Row="0"
            MinHeight="{StaticResource PageHeaderMinHeight}"
            Margin="{StaticResource BottomMediumMargin}"
            Padding="{StaticResource ContentPagePadding}">
            <TextBlock
                x:Name="HeaderText"
                Style="{StaticResource TitleMediumTextBlockStyle}"
                Text="{strings:Resources Key=Home}" />
            <muxc:SplitButton
                x:Name="HeaderOpenButton"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                extensions:SplitButtonExtensions.PrimaryButtonToolTip="{strings:Resources Key=OpenFilesToolTip}"
                extensions:SplitButtonExtensions.SecondaryButtonAccessKey="{strings:KeyboardResources Key=CommandMoreOptionsKey}"
                extensions:SplitButtonExtensions.SecondaryButtonToolTip="{strings:Resources Key=OpenFilesSecondaryToolTip}"
                AccessKey="{strings:KeyboardResources Key=CommandAddOpenFilesKey}"
                AutomationProperties.HelpText="{strings:Resources Key=OpenFilesToolTip}"
                AutomationProperties.Name="{x:Bind HeaderOpenButtonText.Text}"
                Command="{x:Bind Common.OpenFilesCommand}"
                Flyout="{StaticResource OpenFlyout}">
                <StackPanel Orientation="Horizontal">
                    <FontIcon
                        Margin="{StaticResource IconButtonStandardIconMargin}"
                        FontFamily="{StaticResource ScreenboxSymbolThemeFontFamily}"
                        Glyph="{StaticResource FolderOpenGlyph}" />
                    <TextBlock
                        x:Name="HeaderOpenButtonText"
                        Margin="8,0,0,0"
                        Text="{strings:Resources Key=OpenFiles}" />
                </StackPanel>
                <interactivity:Interaction.Behaviors>
                    <behaviors:SplitButtonXYNavigationBehavior />
                    <behaviors:AutoFocusBehavior />
                </interactivity:Interaction.Behaviors>
            </muxc:SplitButton>
        </Grid>

        <!--  Recent-is-empty content  -->
        <Grid
            x:Name="WelcomePanel"
            Grid.Row="1"
            Margin="{x:Bind Common.FooterBottomPaddingMargin, Mode=OneWay}"
            Padding="{StaticResource ContentPagePadding}"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            ColumnSpacing="16"
            Visibility="{x:Bind ViewModel.HasHomeContent, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=true, Mode=OneWay}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.ChildrenTransitions>
                <TransitionCollection>
                    <EntranceThemeTransition />
                </TransitionCollection>
            </Grid.ChildrenTransitions>

            <TextBlock
                x:Name="WelcomePanelIcon"
                Grid.Column="0"
                Margin="0,-16,0,0"
                FontSize="{StaticResource NoContentPanelIconFontSize}"
                Text="👋" />

            <StackPanel
                Grid.Column="1"
                MaxWidth="420"
                VerticalAlignment="Center"
                Orientation="Vertical">
                <TextBlock
                    Margin="0,0,0,4"
                    Style="{StaticResource TitleTextBlockStyle}"
                    Text="{strings:Resources Key=WelcomePanelHeader}" />

                <TextBlock
                    Margin="{StaticResource BottomMediumMargin}"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Text="{strings:Resources Key=WelcomePanelSubtext}"
                    TextWrapping="Wrap" />

                <muxc:SplitButton
                    x:Name="WelcomePanelOpenButton"
                    extensions:SplitButtonExtensions.PrimaryButtonToolTip="{strings:Resources Key=OpenFilesToolTip}"
                    extensions:SplitButtonExtensions.SecondaryButtonAccessKey="{strings:KeyboardResources Key=CommandMoreOptionsKey}"
                    extensions:SplitButtonExtensions.SecondaryButtonToolTip="{strings:Resources Key=OpenFilesSecondaryToolTip}"
                    AccessKey="{strings:KeyboardResources Key=CommandAddOpenFilesKey}"
                    AutomationProperties.HelpText="{strings:Resources Key=OpenFilesToolTip}"
                    AutomationProperties.Name="{strings:Resources Key=OpenFiles}"
                    Command="{x:Bind Common.OpenFilesCommand}"
                    Flyout="{StaticResource OpenFlyout}"
                    Style="{StaticResource AccentSplitButtonStyle}">
                    <StackPanel Orientation="Horizontal">
                        <FontIcon
                            Margin="{StaticResource IconButtonStandardIconMargin}"
                            FontFamily="{StaticResource ScreenboxSymbolThemeFontFamily}"
                            Glyph="{StaticResource FolderOpenGlyph}" />
                        <TextBlock Margin="8,0,0,0" Text="{strings:Resources Key=OpenFiles}" />
                    </StackPanel>
                    <interactivity:Interaction.Behaviors>
                        <behaviors:SplitButtonXYNavigationBehavior />
                        <behaviors:AutoFocusBehavior />
                    </interactivity:Interaction.Behaviors>
                </muxc:SplitButton>
            </StackPanel>
        </Grid>

        <ScrollViewer
            x:Name="DashboardScrollViewer"
            Grid.Row="1"
            ui:ScrollViewerExtensions.VerticalScrollBarMargin="{x:Bind Common.ScrollBarMargin, Mode=OneWay}"
            HorizontalScrollMode="Disabled"
            Visibility="{x:Bind ViewModel.HasHomeContent, Converter={StaticResource BoolToVisibilityConverter}, Mode=OneWay}">
            <StackPanel
                x:Name="DashboardContent"
                Margin="{x:Bind Common.FooterBottomPaddingMargin, Mode=OneWay}"
                Padding="{StaticResource ContentPagePadding}"
                Spacing="24">
                <StackPanel Visibility="{x:Bind ViewModel.HasFavorites, Converter={StaticResource BoolToVisibilityConverter}, Mode=OneWay}">
                    <TextBlock
                        AutomationProperties.HeadingLevel="Level2"
                        Style="{StaticResource SubtitleTextBlockStyle}"
                        Text="{strings:Resources Key=Favorites}" />
                    <GridView
                        Margin="0,12,0,0"
                        IsItemClickEnabled="False"
                        ItemContainerStyle="{StaticResource GridViewItemMediaCardStyle}"
                        ItemTemplate="{StaticResource DashboardTileTemplate}"
                        ItemsSource="{x:Bind ViewModel.FavoriteTiles}"
                        SelectionMode="None" />
                </StackPanel>

                <StackPanel Visibility="{x:Bind ViewModel.HasTags, Converter={StaticResource BoolToVisibilityConverter}, Mode=OneWay}">
                    <TextBlock
                        AutomationProperties.HeadingLevel="Level2"
                        Style="{StaticResource SubtitleTextBlockStyle}"
                        Text="Video tags" />
                    <GridView
                        Margin="0,12,0,0"
                        IsItemClickEnabled="False"
                        ItemContainerStyle="{StaticResource GridViewItemMediaCardStyle}"
                        ItemTemplate="{StaticResource DashboardTileTemplate}"
                        ItemsSource="{x:Bind ViewModel.TagTiles}"
                        SelectionMode="None" />
                </StackPanel>

                <StackPanel Visibility="{x:Bind ViewModel.HasRecentMedia, Converter={StaticResource BoolToVisibilityConverter}, Mode=OneWay}">
                    <TextBlock
                        x:Name="GridViewHeaderText"
                        AutomationProperties.HeadingLevel="Level2"
                        Style="{StaticResource SubtitleTextBlockStyle}"
                        Text="{strings:Resources Key=Recent}" />
                    <GridView
                        x:Name="RecentFilesGridView"
                        Margin="0,12,0,0"
                        ui:ListViewExtensions.Command="{x:Bind ViewModel.PlayCommand}"
                        IsItemClickEnabled="True"
                        ItemContainerStyle="{StaticResource GridViewItemMediaCardStyle}"
                        ItemTemplate="{StaticResource MediaGridViewItemTemplate}"
                        ItemsSource="{x:Bind ViewModel.Recent}"
                        SelectionMode="None">
                        <GridView.Resources>
                            <commands:BindableCommand x:Key="MediaListViewItemPlayCommand" Command="{x:Bind ViewModel.PlayCommand}" />
                        </GridView.Resources>
                        <interactivity:Interaction.Behaviors>
                            <behaviors:ListViewContextTriggerBehavior Flyout="{StaticResource ItemFlyout}" />
                        </interactivity:Interaction.Behaviors>
                    </GridView>
                </StackPanel>

                <StackPanel Visibility="{x:Bind ViewModel.HasVideoFolders, Converter={StaticResource BoolToVisibilityConverter}, Mode=OneWay}">
                    <TextBlock
                        AutomationProperties.HeadingLevel="Level2"
                        Style="{StaticResource SubtitleTextBlockStyle}"
                        Text="Video folders" />
                    <GridView
                        Margin="0,12,0,0"
                        IsItemClickEnabled="False"
                        ItemContainerStyle="{StaticResource GridViewItemMediaCardStyle}"
                        ItemTemplate="{StaticResource DashboardTileTemplate}"
                        ItemsSource="{x:Bind ViewModel.VideoFolderTiles}"
                        SelectionMode="None" />
                </StackPanel>

                <StackPanel Visibility="{x:Bind ViewModel.HasPlaylists, Converter={StaticResource BoolToVisibilityConverter}, Mode=OneWay}">
                    <TextBlock
                        AutomationProperties.HeadingLevel="Level2"
                        Style="{StaticResource SubtitleTextBlockStyle}"
                        Text="{strings:Resources Key=Playlists}" />
                    <GridView
                        Margin="0,12,0,0"
                        IsItemClickEnabled="False"
                        ItemContainerStyle="{StaticResource GridViewItemMediaCardStyle}"
                        ItemTemplate="{StaticResource DashboardTileTemplate}"
                        ItemsSource="{x:Bind ViewModel.PlaylistTiles}"
                        SelectionMode="None" />
                </StackPanel>

                <Border Height="{x:Bind Common.FooterBottomPaddingHeight, Mode=OneWay}" />
            </StackPanel>
        </ScrollViewer>

        <interactivity:Interaction.Behaviors>
            <behaviors:GamepadXYNavigationBehavior />
        </interactivity:Interaction.Behaviors>

        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="NavigationViewStates">
                <VisualState x:Name="Expanded" />
                <VisualState x:Name="Compact" />
                <VisualState x:Name="Minimal">
                    <VisualState.StateTriggers>
                        <triggers:IsEqualStateTrigger Value="{x:Bind Common.NavigationViewDisplayMode, Mode=OneWay}" To="{StaticResource MinimalNavigationViewDisplayMode}" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="HeaderGrid.Padding" Value="{StaticResource ContentPageMinimalPadding}" />
                        <Setter Target="HeaderOpenButtonText.Visibility" Value="Collapsed" />
                        <Setter Target="WelcomePanel.Padding" Value="{StaticResource ContentPageMinimalPadding}" />
                        <Setter Target="WelcomePanel.ColumnSpacing" Value="4" />
                        <Setter Target="WelcomePanelIcon.Margin" Value="0,0" />
                        <Setter Target="WelcomePanelIcon.FontSize" Value="{StaticResource NoContentPanelMinimalIconFontSize}" />
                        <Setter Target="DashboardContent.Padding" Value="{StaticResource ContentPageMinimalPadding}" />
                        <Setter Target="RecentFilesGridView.Padding" Value="{StaticResource GridViewContentPageMinimalPadding}" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>

            <VisualStateGroup x:Name="FlowDirectionStates">
                <VisualState x:Name="LeftToRight" />

                <VisualState x:Name="RightToLeft">
                    <VisualState.StateTriggers>
                        <StateTrigger IsActive="{x:Bind helpers:GlobalizationHelper.IsRightToLeftLanguage}" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="OpenMenuFlyoutFolderItem.Icon" Value="{ui:FontIcon FontFamily={StaticResource ScreenboxSymbolThemeFontFamily}, Glyph={StaticResource FolderListMirroredGlyph}}" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>

            <VisualStateGroup x:Name="SystemEffectsStates">
                <VisualState x:Name="AnimationsEnabled">
                    <VisualState.StateTriggers>
                        <StateTrigger IsActive="{x:Bind Common.AnimationsEnabled, Mode=OneWay}" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="RecentFilesGridView.(ctAnimations:ItemsReorderAnimation.Duration)" Value="{StaticResource ControlNormalAnimationDuration}" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
    </Grid>
</Page>
